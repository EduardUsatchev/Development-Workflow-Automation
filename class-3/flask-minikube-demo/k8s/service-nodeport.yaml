apiVersion: v1
kind: Service
metadata:
  name: flask-svc
  namespace: demo-flask
spec:
  type: NodePort
  selector:
    app: flask-demo
  ports:
    - name: http
      port: 80         # Service port
      targetPort: 5000 # Container port
      nodePort: 30081  # fixed for easy testing
