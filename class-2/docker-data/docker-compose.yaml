version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:5000"
    volumes:
      - ./data:/app/data   # Map local 'data' directory to '/app/data' in container
    environment:
      FLASK_ENV: development